import{db}from"../firebase-config.js";import{collection,getDocs,query,where}from"https://www.gstatic.com/firebasejs/12.14.0/firebase-firestore.js";const out=document.getElementById("audioGrid"),player=document.getElementById("persistentAudio"),mini=document.getElementById("miniPlayer"),title=document.getElementById("miniTitle"),buttons=[...document.querySelectorAll("[data-audio-filter]")],esc=s=>String(s??"").replace(/[&<>"']/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#039;"}[c]));let items=[],filter="all";function spotify(raw){try{const u=new URL(raw);if(!u.hostname.endsWith("spotify.com"))return null;const p=u.pathname.replace(/^\/embed/,"");if(/^\/(episode|show|track)\//.test(p))return "https://open.spotify.com/embed"+p+"?theme=0"}catch{}return null}function card(x){const s=spotify(x.url||"");if(s)return '<article class="audio-card"><span class="audio-type">'+esc(x.audioType||"SPOTIFY")+'</span><h3>'+esc(x.title||"SpeakOut Audio")+'</h3><p>'+esc(x.description||"")+'</p><iframe src="'+esc(s)+'" loading="lazy" title="'+esc(x.title||"SpeakOut audio")+'" allow="autoplay;clipboard-write;encrypted-media;fullscreen;picture-in-picture"></iframe></article>';return '<article class="audio-card"><span class="audio-type">'+esc(x.audioType||"AUDIO")+'</span><h3>'+esc(x.title||"SpeakOut Audio")+'</h3><p>'+esc(x.description||"")+'</p><button class="radio-play" data-url="'+esc(x.url||"")+'" data-title="'+esc(x.title||"SpeakOut Audio")+'">▶ Play</button></article>'}function render(){const a=filter==="all"?items:items.filter(x=>x.audioType===filter);out.innerHTML=a.map(card).join("")}try{const s=await getDocs(query(collection(db,"tvAudio"),where("status","in",["active","published"])));s.forEach(d=>items.push({id:d.id,...d.data()}));render()}catch{out.innerHTML=""}buttons.forEach(b=>b.addEventListener("click",()=>{buttons.forEach(x=>x.classList.remove("active"));b.classList.add("active");filter=b.dataset.audioFilter;render()}));out.addEventListener("click",e=>{const b=e.target.closest(".radio-play");if(!b)return;player.src=b.dataset.url;title.textContent=b.dataset.title;mini.hidden=false;player.play().catch(()=>{});localStorage.setItem("speakout-radio-last",JSON.stringify({title:b.dataset.title,url:b.dataset.url}))});