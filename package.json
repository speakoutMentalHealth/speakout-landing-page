{
  "name": "speakout-landing-page",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/phase*.test.mjs",
    "test:rules": "firebase emulators:exec --project demo-speakout-rules --only firestore \"node --test tests/firestore-rules.emulator.test.mjs\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "5.0.2",
    "firebase": "12.18.0"
  }
}
